server:
  port:0

spring:
  application:
    name: notification-service
  datasource:
    url:
    driver-class-name:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  mail:
    host: smtp.gmail.com
    port: 587
    username: vikasinfo7@gmail.com
    password: bhzz jqni lkdg uxqu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: notification-group
      auto-offset-reset: earliest
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true      
